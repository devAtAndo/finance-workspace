// Empty shim for `server-only`. In a real Next.js build, importing this
// package from a client component throws at build time. In Vitest we don't
// need that guardrail, so we alias to a no-op module.
export {};
